apiVersion: v1
kind: Secret
metadata:
  name: transaction-service-secret
  namespace: default
  labels:
    app: transaction-service
    group: group5-banking
type: Opaque
# Values are base64-encoded. Generate with: echo -n 'value' | base64
# These are placeholder values — replace before production use.
stringData:
  # Internal service API key (used in X-Service-Key header if auth is added)
  SERVICE_API_KEY: "txn-svc-internal-key-change-me"
